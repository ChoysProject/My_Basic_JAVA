package Chapter_04_02;

public class If_and_Switch_05 {

	public static void main(String[] args) {

		
		// Switch문
		
		// switch문은 if문과 마찬가질 조건 제어문이다. 하지만 switch문은 if문처럼 조건식이 true 일 경우 블록 내부의 실행문을
		// 실행하는 것이 아니라, 변수가 어떤 값을 갖느냐에 따라 실행문이 선택된다.
		// if문은 조건식의 결과가 true flase 두 가지밖에 없기 떄문에 경우의 수가 많아질수록 else if를 반복적으로 추가해야 하므로 코드가 복잡해진다.
		// 그러나 switch문의 형식과 실행 흐름을 도식화 한 것이다.
		
		// switch문은 괄호 안의 값과 동일한 값을 갖는 case로 가서 실행문을 실행시킨다.
		// 만약 괄호 안의 값과 동일한 값을 갖는 case가 없으면 default로 가서 실행문을 실행시킨다
		// Example을 찾고 하면된다.
	}

}
